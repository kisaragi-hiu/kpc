#lang info

(define version "0.1")
(define collection 'multi)
(define scribblings (list '("scribblings/kpc.scrbl" ())))
(define pkg-desc "Use @-exp for generic post-processing")
(define pkg-authors '(kisaragi-hiu))

(define compile-omit-paths '("tests"))
